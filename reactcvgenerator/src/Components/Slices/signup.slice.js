import { createSlice } from "@reduxjs/toolkit";
import axios from "axios";

const signupSlice = createSlice({
    name:"signup",
    initialState :{
        status : 0,
        msg : ''
    },
    reducers:{
        
        

    }
})
const {}= signupSlice.actions
export {
}
export default signupSlice.reducer